# Nixpacks configuration for Railway deployment
# Add FFmpeg to the auto-detected Python environment

[phases.setup]
nixPkgs = ["...", "ffmpeg"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
